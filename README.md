# Rock-Paper-Scissors
added index.html file and linked to app.js

added functions to get userChoice and ComputerChoice

added function playRound which takes userChoice and ComputerChoice and
increments the winner's score and console logs the winner.

added function playGame that calls playRound 5 times and delcares the final winner by checking the score.

added UI to game instead of doing in console.

